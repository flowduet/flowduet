# flowduet

占位包（placeholder）。裸名 `flowduet` 为 [FlowDuet](https://github.com/flowduet/flowduet) 项目保留，实际安装请使用 `@flowduet` scope 下的包（如 `@flowduet/core`）。

Placeholder package. The bare name `flowduet` is reserved for the [FlowDuet](https://github.com/flowduet/flowduet) project — real packages are published under the `@flowduet` scope (e.g. `@flowduet/core`).
