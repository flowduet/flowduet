# FlowDuet

双视图流程设计器：钉钉式审批视图与 BPMN 自由画布共享同一棵 BPMN 模型树，导出永远合法可执行。

## 视图与模型

**事实源**:
BPMN 2.0 XML 本身。两个视图与一切编辑操作最终都落在由它解析出的一棵模型树上。
_Avoid_: 主数据、单一数据源

**双视图**:
同一棵模型树的两种呈现——钉钉式视图与 BPMN 视图。双视图的存在前提是共享事实源。
_Avoid_: 双模式、两个设计器

**投影**:
视图与模型树之间的关系：视图是模型树的一种呈现形态，视图间切换不经过任何模型转换。
_Avoid_: 转换、同步

**钉钉式视图**:
纵向递归组件形态的审批流视图，交互参照国内审批流用户的心智（列表加节点、抽屉里配置）。严格限定为 BPMN 可表达子集。
_Avoid_: 审批模式、简易模式

**BPMN 视图**:
自由画布形态的流程视图，面向熟悉 BPMN 的用户。
_Avoid_: 专业模式、流程图模式

## 引擎与适配

**审批流**:
"人怎么审批"的流程领域（会签、条件分支、抄送等），区别于管"服务怎么按流程跑"的服务编排。
_Avoid_: 工作流（过泛）

**方言**:
特定引擎对 BPMN 2.0 的 XML 扩展方式——命名空间前缀 + 扩展属性，如 flowable 方言。
_Avoid_: 私有格式、引擎格式

**引擎适配器**:
把内核标准 BPMN 输出适配成特定引擎方言的可插拔模块，收敛于三点：命名空间前缀、扩展属性 schema、任务类型映射。
_Avoid_: 引擎支持文件、引擎插件

**合法可执行**:
产品的硬承诺：设计器画出的任何流程导出即为能被目标引擎真实部署执行的 BPMN XML。
_Avoid_: 合法 XML（弱化了可执行）

## 测试不变量

**往返保真**:
`parse(xml) → model → compile()` 语义等价的不变量，全项目最高优先级测试不变量。只承诺设计器画出的内容无损，不承诺任意第三方文件全细节无损导入。
_Avoid_: 无损转换

**编译合同**:
`compile(model)` 的输出契约：对给定输入流程，产出与基准文件逐字一致的引擎方言 XML。
_Avoid_: 快照测试

**适配器合同**:
引擎适配器接口的验收用例：任一适配器实现必须满足命名空间前缀、扩展属性注入点、任务类型映射三点的合同测试。
_Avoid_: 兼容层测试

**基准文件**:
编译合同比对用的黄金样本 XML，人工编写、部署冒烟兜底其合法性。
_Avoid_: golden file（对外文档统一用中文）

**部署冒烟**:
把编译产物经引擎 REST 接口真实部署成功的物理验证，是"合法可执行"承诺的定海神针。
_Avoid_: 集成测试（过泛）
