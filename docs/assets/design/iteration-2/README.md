# 迭代二 UI 方向澄清图

2026-09-20 开发前视觉澄清（grilling 出图循环：每面 2 变体 → 选向 → 细化定稿）。**定位是方向澄清，不是像素规格**——组件面（抽屉、表单控件）的精确观感以 Element Plus 实组件为准；图中中文文案是 AI 生成，可能有瑕疵。

| 文件 | 面 | 定稿方向 |
| --- | --- | --- |
| `dingtalk-editor.png` | 钉钉式编辑视图 | 着色容器式：浅靛蓝圆角容器 + 左侧竖轨 + 小标签表达条件/并行分支嵌套，卡间「+」插入按钮 |
| `approval-drawer.png` | 审批节点抽屉 | 分段选项卡式：字段纵向分段，完成方式为三张可选卡片（会签/或签/依次），多人时该段出现 |
| `bpmn-readonly-canvas.png` | BPMN 只读投影 | 经典 BPMN 词汇 + 现代化呈现：圆角描边、柔和阴影、珊瑚橙强调（多实例标记/选中态），不卡片化、不着色分区 |

双色声部（ADR-0008）：钉钉式视图 = 靛蓝 `#2D3E97` 声部；BPMN 画布 = 珊瑚橙 `#F76547` 声部。

决策已回引 `docs/specs/iteration-2/dingtalk-view-mvp.md` 与 `bpmn-readonly-projection.md`；首轮 6 张候选变体未入仓（仅定稿入册）。
